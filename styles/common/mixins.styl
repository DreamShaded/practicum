@import 'variables.styl';
@import "stylus-mixins.styl";

debug()
  background-color rgba(red, .33) !important

outlineDebug()
  outline 3px solid rgba(red, .33) !important

flexCenter()
  display flex
  flex-direction column
  align-items center
